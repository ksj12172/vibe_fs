{
  "buildCommand": "prisma generate --no-engine && next build",
  "functions": {
    "app/api/**/*.js": {
      "maxDuration": 30
    },
    "api/**/*.py": {
      "runtime": "python3.9",
      "maxDuration": 30
    }
  }
}
